'use strict';

const test = require('ava');

const viziaFormat = require('../src');

/**
 * Output function
 */

const rendererStub = (component, props, children) => ({component, props, children});

test('Custom Output function', (t) => {
    t.deepEqual(
        viziaFormat(500, {format: 'currency', currency: 'GBP', output: rendererStub}),
        {
            component: 'span',
            props: {className: 'viz-format'},
            children: [
                {
                    component: 'span',
                    props: {className: 'viz-format__currency'},
                    children: '£'
                },
                '500'
            ]
        },
        'returns element structure'
    );
});
