'use strict';

const test = require('ava');

const createValidator = require('@vizia/json-schema-utils');

const schema = require('../../schema');

test('Schema', (t) => {
    t.notThrows(() => {
        createValidator(schema);
    }, 'schema is valid');
});
